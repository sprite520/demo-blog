spring:
  thymeleaf:
    cache: false
    mode: HTML
    encoding: utf-8
    prefix: classpath:/templates/
    suffix: .html
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&userSSL=false
    username: root
    password: a7w8e9zz
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: info
    rhr: debug
  file:
    path: log/blog.log
server:
  max-http-header-size:
    102400

